---
import Layout from "../layouts/Layout.astro";
import MainContainer from "../layouts/MainContainer.astro";
import ChatWindow from "../components/ChatWindow.astro";
import ChatBubble from "../components/ChatBubble.astro";
import ChatTimestamp from "../components/ChatTimestamp.astro";

const messages = [
  "Of course, I would have known about HTML, CSS, JavaScript and Git. I can work with React.js, Vue.js and a bit of Svelte.",
  "Oh, and I prefer TypeScript to JavaScript any day of the week.",
  "On the back-end side, I can use Node.js and Express to make REST API.",
  "These days, I like to work with PHP and Laravel more than Node.js.",
];
---

<Layout title="Skills">
  <MainContainer>
    <ChatWindow>
      <ChatBubble
        isBelongToGuest
        message="What skills do you have as a web developer?"
      />
      {messages.map((m) => <ChatBubble message={m} />)}
      <ChatTimestamp time="3m" />
    </ChatWindow>
  </MainContainer>
</Layout>
